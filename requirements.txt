# Core dependencies for converters and bundle scripts
numpy
pandas
pyarrow
geopandas
shapely
tifffile
scikit-image
scipy
tqdm

# Segmentation methods
# Segger: install from vendored source (includes local modifications)
#   pip install -e segger_dev/
# BIDCell: install from PyPI
bidcell
